/// <reference types="node" />
import { Benchmark, IReporter, IReporterFactory } from '.';
export declare const prettyFactory: IReporterFactory;
export declare class PrettyReporter implements IReporter {
    private readonly out;
    private readonly numberFormat;
    private readonly results;
    constructor(out: NodeJS.WriteStream);
    onStartCycle(benchmark: Benchmark): void;
    onFinishCycle(newResult: Benchmark): void;
    onComplete(): void;
}
