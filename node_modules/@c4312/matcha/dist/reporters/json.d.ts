/// <reference types="node" />
import { Benchmark, IReporter, IReporterFactory } from '.';
export declare const jsonFactory: IReporterFactory;
export declare class JsonReporter implements IReporter {
    private readonly out;
    private printed;
    constructor(out: NodeJS.WriteStream);
    onStartCycle(): void;
    onFinishCycle(benchmark: Benchmark): void;
    onComplete(): void;
}
