/// <reference types="node" />
import { Benchmark, IReporter, IReporterFactory } from '.';
export declare const jsonSummaryFactory: IReporterFactory;
export declare class JsonSummaryReporter implements IReporter {
    private readonly out;
    private readonly results;
    constructor(out: NodeJS.WriteStream);
    onStartCycle(): void;
    onFinishCycle(newResult: Benchmark): void;
    onComplete(): void;
}
