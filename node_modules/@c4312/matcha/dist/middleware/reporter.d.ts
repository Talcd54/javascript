import { Middleware } from '../runner';
import { IReporter } from '../reporters';
/**
 * A middleware that instruments a reporter.
 */
export declare const reporterMiddleware: (reporter: IReporter) => Middleware;
