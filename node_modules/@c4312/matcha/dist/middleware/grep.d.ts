import { Middleware } from '../runner';
export declare const grepMatches: (pattern: string | RegExp, name: string) => boolean;
/**
 * A middleware that only runs benchmark cases matching
 * the given pattern.
 */
export declare const grepMiddleware: (pattern: string | RegExp) => Middleware;
